part of 'theme_cubit.dart';

/// Represents the state for managing the theme brightness in the application.
class ThemeState {
  /// Represents the brightness mode (light or dark) for the theme.
  final Brightness brightness;

  /// Constructs a [ThemeState] instance with the specified [brightness].
  const ThemeState({required this.brightness});
}
